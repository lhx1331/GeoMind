# 🚀 GeoMind 运行前检查

## ✅ 当前状态

根据检查脚本结果，您的项目状态：

| 项目 | 状态 | 说明 |
|------|------|------|
| Python 版本 | ✅ 通过 | Python 3.11.9 |
| 依赖包 | ❌ 未通过 | 需要安装依赖 |
| CLI 工具 | ❌ 未通过 | 需要安装包 |
| 配置文件 | ❌ 未通过 | 需要配置 API Keys |
| GeoCLIP 模型 | ✅ 通过 | 模型已下载 |
| 示例图片 | ⚠️ 可选 | 可以稍后添加 |

---

## 📋 运行前必做清单

### 1️⃣ 安装依赖（必需）

```bash
# 安装项目依赖
pip install -e .

# 如果遇到问题，可以尝试：
pip install -e ".[dev]"
```

**预计时间**: 2-5 分钟

---

### 2️⃣ 配置 API Keys（必需）

编辑 `config.yaml` 文件，填写以下内容：

```yaml
# 第 14 行 - DeepSeek API Key
deepseek_api_key: "sk-你的DeepSeek密钥"

# 第 29 行 - GLM-4V API Key  
glm_api_key: "你的GLM密钥"
```

**获取 API Keys**:
- DeepSeek: https://platform.deepseek.com/
- GLM-4V: https://open.bigmodel.cn/

**预计时间**: 5 分钟

---

### 3️⃣ 验证配置（推荐）

运行配置测试：

```bash
python test_config.py
```

如果看到 "🎉 恭喜！所有配置测试通过"，说明配置成功！

---

## 🎯 运行方式

### 方式 1: 使用 CLI（推荐）

```bash
# 单张图片定位
geomind locate --image path/to/image.jpg

# 详细输出
geomind locate --image path/to/image.jpg --verbose

# 保存结果到文件
geomind locate --image path/to/image.jpg --output result.json
```

---

### 方式 2: 使用 Python API

```python
from geomind import GeoMindAgent

# 创建 Agent
agent = GeoMindAgent()

# 执行地理定位
result = await agent.geolocate("path/to/image.jpg")

# 查看结果
print(f"位置: ({result.lat}, {result.lon})")
print(f"置信度: {result.confidence:.2%}")
```

---

### 方式 3: 启动 API 服务

```bash
# 启动 FastAPI 服务
python -m geomind.api.app

# 或使用 uvicorn
uvicorn geomind.api.app:app --host 0.0.0.0 --port 8000
```

然后访问: http://localhost:8000/docs

---

## 📝 快速测试

### 测试 1: 运行检查脚本

```bash
python check_ready.py
```

所有项目显示 `[OK]` 和 `通过` 即可运行！

---

### 测试 2: 运行示例代码

```bash
# 基础示例
python examples/use_geomind_agent.py

# 完整工作流示例
python examples/complete_phrv_workflow.py
```

---

## ⚠️ 常见问题

### 问题 1: `ModuleNotFoundError`

**原因**: 依赖未安装

**解决**:
```bash
pip install -e .
```

---

### 问题 2: `API Key 未配置`

**原因**: `config.yaml` 中的 API Keys 未填写

**解决**: 编辑 `config.yaml`，填入真实的 API Keys

---

### 问题 3: `GeoCLIP 模型未找到`

**原因**: GeoCLIP 模型未下载

**解决**:
```bash
python download_geoclip.py
```

---

### 问题 4: CLI 命令不存在

**原因**: 包未安装

**解决**:
```bash
pip install -e .
```

然后重新打开终端。

---

## 🎉 完成检查后

当所有检查通过后，您可以：

1. **运行 CLI**: `geomind locate --image <图片>`
2. **运行 Python**: `python examples/use_geomind_agent.py`
3. **启动 API**: `python -m geomind.api.app`

---

## 📚 更多帮助

- **快速开始**: `快速开始.md`
- **配置指南**: `配置指南.md`
- **API 文档**: `docs/API_KEYS_CHECKLIST.md`
- **CLI/API 使用**: `CLI_API_使用指南.md`

---

**需要帮助？** 运行 `python check_ready.py` 查看详细状态！


