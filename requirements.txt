# GeoMind 核心依赖
# 从 pyproject.toml 生成，用于兼容性
# 推荐使用: pip install -e .

# 核心框架
langchain>=0.1.0
langgraph>=0.0.20
langchain-core>=0.1.0
langchain-openai>=0.0.5
langchain-anthropic>=0.1.0

# 数据验证和状态管理
pydantic>=2.5.0
pydantic-settings>=2.1.0

# 图像处理
pillow>=10.0.0
opencv-python>=4.8.0
exifread>=3.0.0

# HTTP 客户端
httpx>=0.25.0
aiohttp>=3.9.0

# 工具和工具链
python-dotenv>=1.0.0
pyyaml>=6.0
tenacity>=8.2.0

# 日志和监控
structlog>=23.2.0
rich>=13.7.0

# 注意: MCP 和 e2b 需要根据实际情况安装
# mcp>=0.1.0
# e2b>=0.1.0

